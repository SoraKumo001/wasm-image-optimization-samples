{
  "name": "deno-wasm-image-optimization",
  "version": "1.0.0",
  "main": "index.js",
  "author": "SoraKumo <info@croud.jp>",
  "license": "MIT",
  "scripts": {
    "dev": "deno run --watch --allow-all src/index.ts",
    "update": "deno cache --reload src/index.ts",
    "deploy": "npx next-exec -- deployctl deploy --project deno-image --build --include=./src --prod"
  }
}
